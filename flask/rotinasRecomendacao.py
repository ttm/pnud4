import auxiliar
def recomendaParticipante(destinatario, idd, metodo="hib",polaridade="mis"):
    """Caso o método seja top, usar somente a rede.
    Caso o método seja tex, usar somente o texto produzido.
    Caso o métodos seja hib, usar os dois."""
    # montar as duas redes, deixar no builtin
    # incluir a montagem das redes e o bag o words jah na triplificação
    return "ba"

